<%= provide(:title, "Conversation") %>
<h1>Messages</h1>
<ul class="list-group">
	<%= render partial: 'conversations/conversation', collection: @conversations %>
</ul>
<p><%= link_to 'Start conversation', new_message_path, class: 'btn btn-md btn-primary' %></p>
<%= will_paginate @conversations %>